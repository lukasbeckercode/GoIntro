package strutil //this is the packages Name

func Reverse(s string) string { //Thanks to stackOverflow for this code
	runes := []rune(s)
	for i, j := 0, len(runes)-1; i < j; i, j = i+1, j-1 {
		runes[i], runes[j] = runes[j], runes[i]
	}
	return string(runes)
}
